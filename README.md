# JS-learn
A code repo for javascript

## Setup
1. Ensure Node.js is installed (version 24.11.1 or later).
2. Install dependencies: `npm install`
3. Run the project: `node index.js`

## Development
- Edit `index.js` to add your JavaScript code.
- Use `npm test` for testing (currently placeholder). 
